[project]
name = "mqtt-gateway-template"
version = "0.1.0"
description = "Template MQTT gateway"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["paho-mqtt>=2.1.0", "python-dotenv>=1.2.2"]
